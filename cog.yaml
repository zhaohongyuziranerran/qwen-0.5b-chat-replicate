# Cog Configuration for Replicate
# https://github.com/replicate/cog

build:
  gpu: true
  python_version: "3.11"
  python_packages:
    - torch>=2.0.0
    - transformers>=4.36.0

predict: "predict.py:Predictor"
